name: terraform-plan

# PR gate (/workflow/github-actions): checkov → fmt → init → validate → plan → comment.
# A PR into develop plans the staging workspace; a PR into main plans production.
# TFC is the state backend (Local execution — GitHub runs the plan); AWS access via OIDC.

on:
  pull_request:
    branches: [develop, main]
    paths:
      - "terraform/**"
      - ".github/workflows/terraform-plan.yml"

permissions:
  contents: read
  pull-requests: write
  id-token: write # OIDC → AWS (plan refreshes data sources / state)

concurrency:
  group: terraform-plan-${{ github.ref }}
  cancel-in-progress: true

env:
  TF_TOKEN_app_terraform_io: ${{ secrets.TFC_API_TOKEN }}
  # Workspace + tfvars follow the PR target branch. cloud{} selects the workspace by TF_WORKSPACE.
  TF_WORKSPACE: tadeumendonca-iac-${{ github.base_ref == 'main' && 'production' || 'staging' }}
  VAR_FILE: ${{ github.base_ref == 'main' && 'env/prd.tfvars' || 'env/stg.tfvars' }}
  # Node 20 is deprecated on runners (forced off 2026-06-16). Run bundled JS actions on Node 24.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  plan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Static analysis — hard-fail gate. Suppressions are curated: global policy deviations in
      # .checkov.yaml, resource-specific ones inline as #checkov:skip on the resource.
      # (OSS checkov can't gate by severity — HIGH needs the Prisma/Bridgecrew key — so we fail on
      # any unsuppressed finding, which is stricter than "block on HIGH".)
      - name: Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: terraform/
          framework: terraform
          config_file: .checkov.yaml
          soft_fail: false
          output_format: cli

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Format check
        run: terraform fmt -check -recursive

      - name: Init
        run: terraform init -input=false

      - name: Validate
        run: terraform validate -no-color

      # tee to a file (pipefail preserves terraform's exit code → steps.plan.outcome). The comment step
      # reads this file instead of an env var: a large plan (e.g. a 100+-resource destroy) overflows the
      # execve arg+env limit and the github-script node process fails to start with "Argument list too
      # long" BEFORE it can truncate. Reading from a file keeps the value out of the process environment.
      - name: Plan
        id: plan
        shell: bash
        run: |
          set -o pipefail
          terraform plan -input=false -no-color -var-file="$VAR_FILE" 2>&1 | tee plan.txt

      - name: Comment plan on PR
        if: always()
        uses: actions/github-script@v7
        env:
          WORKSPACE: ${{ env.TF_WORKSPACE }}
        with:
          script: |
            const fs = require("fs");
            const max = 60000;
            const planPath = "terraform/plan.txt";
            let plan = fs.existsSync(planPath) ? fs.readFileSync(planPath, "utf8") : "(no plan output)";
            if (plan.length > max) plan = plan.slice(0, max) + "\n...(truncated — see the Actions log)";
            const body = [
              `### Terraform Plan — \`${process.env.WORKSPACE}\``,
              `Status: \`${{ steps.plan.outcome }}\``,
              "```hcl",
              plan,
              "```",
            ].join("\n");
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number,
            });
            const marker = "### Terraform Plan — `" + process.env.WORKSPACE + "`";
            const existing = comments.find((c) => c.body && c.body.startsWith(marker));
            if (existing) {
              await github.rest.issues.updateComment({ owner: context.repo.owner, repo: context.repo.repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number, body });
            }

      - name: Fail if plan failed
        if: steps.plan.outcome == 'failure'
        run: exit 1
